#include "GiftProtectedMode.h"

//--------------------------------------------------------------------------------------------
// c-tor
GiftProtectedMode::GiftProtectedMode(const sf::Vector2f& location) :Gift(location)
{
}
